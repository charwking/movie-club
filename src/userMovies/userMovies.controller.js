(function (angular) {
    'use strict';

    angular
        .module('movieClub')
        .controller('UserMoviesController', UserMoviesController);

    function UserMoviesController() {
    }

}(window.angular));
