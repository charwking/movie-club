(function (angular) {
    'use strict';

    angular
        .module('movieClub.core', [
            'templates-main',
            'utils.router',
            'ui.router'
        ]);

}(window.angular));
