(function (angular) {

    angular
        .module('movieClub')
        .run(appRun);

    function appRun(Analytics) {
        // Analytics injected to enable automatic page tracking
    }

}(window.angular));
