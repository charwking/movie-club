(function (angular) {
    'use strict';

    angular
        .module('movieClub.dashboard', [
            'movieClub.core',
            'movieClub.properties',
            'firebase'
        ]);

}(window.angular));
